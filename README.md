The function `eply()` is like `apply()` except that the dataset is a bunch of expressions that are lazily evaluated into function arguments. For more, see the vignette.

```r
library(eply)
vignette("eply")
```
